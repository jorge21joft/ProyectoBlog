<% @posts.each do |post| %>
    <h1><%= link_to post.titulo, post %></h1>

        <div>
       <% if post.user.email == current_user.email %>
        <%= post.body %> - <%= link_to  "eliminar", post, class:"red", method: :delete   %>
     <%end%>
    </div>

<%end%>